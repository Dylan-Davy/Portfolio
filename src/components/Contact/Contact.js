import React from 'react';
import styles from './Contact.module.scss';

const Contact = (props) => {
    return (
        <div id="Contact" className={styles.Contact}>
            <h1 className={'header--primary'}>Contact</h1>
            
        </div>
    )
}

export default Contact
